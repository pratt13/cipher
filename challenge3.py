from BaseCipher import BaseCipher


class ChallengeThree(BaseCipher):
    
    def decoding_data(self, data, keyword):
        """The task is to implement this method.
        The data is shuffled by some unknown constant amount.
        All that is known, that the keyword exists in the solution.
        """
        raise NotImplementedError